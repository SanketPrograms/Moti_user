import 'package:flutter/material.dart';

class review_rating_mainScreen extends StatelessWidget {
  const review_rating_mainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child:Column(
        children: [
          
        ],
      )
    );
  }
}
